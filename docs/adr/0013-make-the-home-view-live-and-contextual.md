# Make the home view live and contextual

The no-argument entry point will show the executable, connected instance, authenticated identity, repository context, and compact live issue/pull aggregates rather than usage text. Capability discovery and generated context remain separate bounded commands, allowing the same repository-scoped guidance to feed on-demand Agent Skills and opt-in session hooks without embedding credentials.
